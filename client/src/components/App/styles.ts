import { pxToRem } from '../../utils/pxToRem';

export const styles = {
  btnGroupContainer: { margin: '400px auto', textAlign: 'center' },
  typography: { flexGrow: 1, display: { xs: 'none', sm: 'block' } },
};
